{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for HotSeaters. Conservative: window + dialog only. No shell exec, no broad filesystem access. All persistence flows through PGlite (IndexedDB) inside the WebView.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:webview:default",
    "core:app:default",
    "core:event:default",
    "core:path:default",
    "core:resources:default",
    "core:menu:default",
    "core:tray:default"
  ]
}
