# Tasks — change-V11

- [ ] T1. Consolidate the V03–V10 drift backlog; group by primitive vs page.
- [ ] T2. Fix systemic primitive defects in `src/components/ui/*` + `src/index.css` (RULE 0.3).
- [ ] T3. Fix per-page copy/section/link defects.
- [ ] T4. Root-cause font/color defects via `getComputedStyle` (RULE 0.4), not source CSS.
- [ ] T5. Re-run VR for every touched surface; confirm drift drops.
- [ ] T6. `pnpm typecheck && pnpm lint && pnpm test` green.
