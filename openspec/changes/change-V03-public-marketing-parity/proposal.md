# change-V03 — Public/marketing surface parity audit + VR

## Why
W1 shipped the public surface without per-page RULE-0 audit. Prove it.

## What changes
Per-page RULE-0 audit note + extend `bible-vs-port` drift coverage for:
`/` (Landing/Home), `/Landing`, `/Pricing`, `/PrivacyPolicy`,
`/TermsOfService`, `/ReferralLanding`. Record drift % at desktop + mobile.
File any drift defect into the V11 remediation backlog.

## Impact
Test + audit-ledger only (unless a defect is fixed inline and trivial).
Depends on V01 (extended harness) + V02 (audit ledger).
